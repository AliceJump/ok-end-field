[project]
name = "ok-end-field"
version = "0.1.0"
description = "Endfield game automation built on ok-script"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ok-script==2.0.2",
    "PySide6-Essentials==6.11.1",
    "PySide6-Fluent-Widgets==1.10.5",
    "PySideSix-Frameless-Window==0.7.3",
    "onnxocr-ppocrv5==0.0.14",
    "openvino==2026.0.0",
    "opencv-python==4.12.0.88",
    "websockets==15.0.1",
    "json5==0.15.0",
    "pynput==1.8.2",
    "pyautogui==0.9.54",
]

[dependency-groups]
dev = [
    "polib",
    # gen_release_requirements.py 解析环境标记所需（原先由 scikit-image 传递引入）
    "packaging",
]

[tool.uv]
package = false

[tool.ruff.lint]
allowed-confusables = ["，", "。", "；", "：", "（", "）", "、"]
